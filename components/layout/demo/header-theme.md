## zh-CN

使用 `theme` 属性设置顶部布局的主题颜色。`light` 主题会自动跟随当前主题算法，`dark` 主题会使用深色背景和高对比度文字。

## en-US

Use the `theme` property to set the header color theme. The `light` theme follows the current theme algorithm, while the `dark` theme uses a dark background with high-contrast text.
